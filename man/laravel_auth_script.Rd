% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ui.R
\name{laravel_auth_script}
\alias{laravel_auth_script}
\title{Browser-side Laravel auth handshake script}
\usage{
laravel_auth_script(app_url = Sys.getenv("LARAVEL_APP_URL"))
}
\arguments{
\item{app_url}{Origin the session id is posted to (the Laravel host). Defaults
to the \code{LARAVEL_APP_URL} environment variable.}
}
\value{
A \link[shiny:tags]{shiny::tags} \code{script} node.
}
\description{
Returns a \verb{<script>} tag that registers a Shiny custom message handler named
\code{"session"}. When the Shiny server sends the session id (via
\code{\link[=laravel_auth]{laravel_auth()}}), the handler forwards it to the Laravel parent window with
\code{parent.postMessage()}, targeting \code{app_url} as the message origin.
}
\details{
Drop the returned tag into the app's \verb{<head>}, e.g.
\code{tags$head(laravel_auth_script())}.
}
\examples{
\dontrun{
shiny::tags$head(laravel_auth_script())
}
}
