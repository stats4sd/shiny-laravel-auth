% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth.R
\name{laravel_auth}
\alias{laravel_auth}
\title{Run the server-side Laravel <-> Shiny authentication handshake}
\usage{
laravel_auth(
  session,
  on_authenticated = NULL,
  sessions_dir = "../.sessions",
  base_url = Sys.getenv("URL"),
  obj_name = "auth"
)
}
\arguments{
\item{session}{The Shiny \code{session} object.}

\item{on_authenticated}{Optional zero-argument function called inside the
\code{POST} filter the moment authentication is confirmed — e.g. to swap the
pre-auth UI for the real UI. Keeps this package UI-agnostic.}

\item{sessions_dir}{Directory holding the per-session callback files. Must be
readable/writable by both the Shiny and Laravel processes. Defaults to
\code{"../.sessions"}.}

\item{base_url}{This Shiny app's externally reachable base URL, prepended to
the callback path that Laravel will \code{POST} to. Defaults to the \code{URL}
environment variable.}

\item{obj_name}{Name passed to \code{session$registerDataObj()}; parameterised so
multiple apps can coexist under one \code{sessions_dir}.}
}
\value{
A \link[shiny:reactiveValues]{shiny::reactiveValues} object with \code{user} and \code{input} fields,
populated once Laravel posts back.
}
\description{
Call this once inside the Shiny server function. It:
\enumerate{
\item registers a private data object, minting a unique callback URL
\verb{/session/\{uuid\}/dataobj/\{id\}} whose filter responds to a \code{POST};
\item writes that callback URL to \code{{sessions_dir}/{uuid}} — a file on a
volume shared with Laravel, so Laravel can find where to call back;
\item sends \code{{uuid}} to the browser via the \code{"session"} custom message
(handled by \code{\link[=laravel_auth_script]{laravel_auth_script()}}), which posts it up to Laravel;
\item removes the session file when the Shiny session ends.
}
}
\details{
When Laravel has authorised the user it \code{POST}s the context data back to the
callback URL. That \code{POST} triggers the filter: \code{on_authenticated()} is called,
the posted body is parsed into \code{auth$input}, and \code{auth$user} is set — so an
\code{observeEvent(auth$user, ...)} can initialise the app using \code{auth$input}.

There are no signed tokens: trust rests on the shared filesystem (only this
process can create a given \code{{uuid}} file), browser origin checks on the
Laravel side, and Laravel performing authorisation before it calls back.
}
\examples{
\dontrun{
auth <- laravel_auth(session, on_authenticated = render_authenticated_ui)
observeEvent(auth$user, {
  investment_id <- auth$input$investment_id
  # ...initialise the app...
})
}
}
